/target
*.webp
*.png
!assets/reference/*.png
render_final.log
*Zone.Identifier
/tools/osgedit/renders/
# Raw screen recordings stay local (16-40 MB each); the small
# isolated reference clips in videos/clips/ ARE tracked.
videos/*
!videos/clips/
# GRIPITT strike-reel render output (generated by the exporter that now
# lives in the gripitt clone) — never commit.
/gripitt_strike.mp4
